/*
* Built-in function
*
* TODO apply:
* 1. apply fun return value is always the value itself it would not change
* 2. apply fun has this == value itself
*
* TODO let:
* 1. let return value will be changed according to the last line
* 2. let fun has it == value itself
*
* TODO run:
*1. the return value will be changed according to the last line
*2. run has this == value itself
*
*TODO with with(str)
*1. the return value will be changed according to the last line
*2. with has this == value itself
*
*TODO also str.also
*
*1. the return value would not change
*2. also has it = value(str) itself
*
*/